package tiendaparking;

import tiendaparking.controller.ControladorRegistro;

public class TiendaParking {
    public static void main(String[] args) {
        ControladorRegistro controlador = new ControladorRegistro();
        controlador.iniciarRegistro();
    }
}
